const initialState = {
  inputList: ['0'],
  outputList: []
};

export function calculatorReducer(state = initialState, { type, payload } = {}) {
  // if (type === LOCATION_CHANGE) {
  //   return { ...state, locationBeforeTransitions: payload }
  // }

  return state
}
